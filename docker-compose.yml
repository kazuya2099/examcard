# docker-compose.yml（開発環境のDB用のみ）
services:
  db:
    image: postgres:16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: examcard
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data: